machine:
 java:
  version: oraclejdk8

test:
 override:
  - emulator -avd circleci-android24 -no-window:background:true parallel:true
  - circle-android wait-for-boot
  - ./gradlew assembleDebug assembleRelease
  - ./gradlew connectedAndroidTest
